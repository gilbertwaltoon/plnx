See 'Everythings Webserver.odt' labook.

Cd into this dir and do

$ $ busybox httpd -p 8080 -f -v

This starts Buysbox httpd on port 8080 in verbose mode
 
(It all seizes-hold of the terminal until a Ctrl-C is pressed.)